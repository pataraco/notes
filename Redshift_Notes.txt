Redshift Notes
--------------


- brew install pgcli
- pgcli --version

- pgcli usage
usage: "pgcli postgresql://[user[:password]@][netloc][:port][/dbname]"

- pgcli example
pgcli postgresql://USER:PASSWORD@CLUSTER_NAME.UID.REGION.redshift.amazonaws.com:PORT/DB"

pgcli postgresql://admin:r3^ua%Z43j@dev.ryfig7s7v829.us-west-2.redshift.amazonaws.com:5439/dev

$ ssh -A -L LOCAL_PORT:REMOTE_HOST:REMOTE_PORT USER@HOST  # SSH tunnel
$ pgcli -h HOST -p PORT -u USER -d DB

$ ssh -A -L 5439:dev.ryfig7s7v829.us-west-2.redshift.amazonaws.com:5439 raco@bastion  # SSH tunnel
$ pgcli -h localhost -p 5439 -u admin -d dev

